{
  "name": "orbital-mission-control",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:pages": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "preview:pages": "vite preview --host 127.0.0.1",
    "check": "node --check app/main.js && node --check app/mission-control.js && node --check app/globe.js && node --check app/model.js && node --check app/shell.js"
  },
  "dependencies": {
    "jquery": "4.0.0"
  },
  "devDependencies": {
    "vite": "8.0.13"
  }
}
